<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tarefas.android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tarefas Android"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Tarefa3.ConsultarActivity"></activity>
        <activity android:name=".Tarefa3.MenuActivity" />
        <activity android:name=".Tarefa2.LogadoFilmes" />
        <activity android:name=".Tarefa3.Tarefa3Activity" />
        <activity android:name=".Tarefa2.Tarefa2Activity" />
        <activity android:name=".Tarefa1.Tarefa1Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>